!!!
%html
  %head
    %meta{:content => "text/html; charset=UTF-8", "http-equiv" => "Content-Type"}/
    %title LiveTraveller
    = csrf_meta_tags
    = csp_meta_tag
    = stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload'
    = javascript_include_tag 'application', 'data-turbolinks-track': 'reload'
  %body
    %header
      .headerTop
        .headerTopLogo
          =link_to root_path do
            =image_tag "logo.png"
        %i.fas.fa-bars#drawrBtn
    .drawr
      .drawrTop
        %i.far.fa-window-close#closeBtn
        - if user_signed_in? && @user.icon.present?
          = image_tag @user.icon.url, class:"drawrTopImage"
        - else
          = image_tag "カメラアイコン.png", class:"drawrTopImage"
      .drawrCenter
        .drawrCenterList
          =link_to "スケジュール予約", new_life_path
        .drawrCenterList
          =link_to "アーティスト登録", new_artist_path
        .drawrCenterList
          =link_to "グッズ", new_good_path
        .drawrCenterList
          =link_to "ログイン", new_user_session_path
        .drawrCenterList
          =link_to "新規登録", new_user_registration_path
        .drawrCenterList
          =link_to "ログアウト", destroy_user_session_path, method: :delete
        .drawrCenterList
          =link_to "グループ作成", new_group_path
        .drawrCenterList
          =link_to "グループ一覧", groups_path
    .main
      .mainContent
        = yield
